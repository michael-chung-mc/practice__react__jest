import React from 'react'
import ReactDOM from 'react-dom/client'
import App from '../App.jsx'
import QuestionAdd from '../QuestionAdd/QuestionAdd.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <QuestionAdd />
  </React.StrictMode>,
)
